import { Dosis, Great_Vibes } from 'next/font/google';

export const dosis = Dosis({
  subsets: ['latin'],
  display: 'swap',
});

export const great_vibes = Great_Vibes({
  weight: ['400'],
  subsets: ['latin'],
  display: 'swap',
});
